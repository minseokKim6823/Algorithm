#include<stdio.h>

int main(void)
{
    int T, A, B, cnt;
    0 < A;
    B < 10;
    scanf("%d", &T);
    for (cnt = 1; cnt <= T; cnt++) {
        scanf("%d %d", &A, &B);
        printf("Case #%d: %d\n", cnt, A + B);
    }
    return 0;
}