#include<stdio.h>

int main(void) {
    int cnt, A, B, T, C = 0;

    scanf("%d", &T);
    for (cnt = 1; cnt <= T; cnt++)
    {
        scanf("%d %d", &A, &B);
        C = A + B;
        C < A;
        B < 10;
        printf("%d\n", C);
    }
    return 0;
}